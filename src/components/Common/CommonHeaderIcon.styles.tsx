import * as RN from 'react-native';
import theme from '../../utils/theme';

export default RN.StyleSheet.create({
  iconContainer: {
    backgroundColor: theme.colors.primary.mediumBlack,
    borderRadius: theme.borderRadius,

    padding: 6,
  },
});
